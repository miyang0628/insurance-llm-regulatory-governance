# 논문 A(uw-benchmark)와 동일 conda 환경 사용
# 아래는 논문 B 추가 패키지

httpx>=0.27.0        # 망분리 실험: HTTP 패킷 분석
pyyaml>=6.0          # Semantic Layer yaml 파싱
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tabulate>=0.9.0      # 결과 표 출력
